<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs') %>
  <body>
    <%- include('./partials/sidebar.ejs') %>
    <main>
      <h1><%= title %></h1>

      <ul>
        <% for (let i = 0; i < book_list.length; i++) { %> <% if
        (book_list.length === 0) {%>
        <li>There are no books.</li>
        <%} else {%>
        <li>
          <a href="book.url"><%= book_list[i].title %></a>
          (<%=book_list[i].author.name%>)
        </li>
        <%}%> <%}%>
      </ul>
    </main>
  </body>
</html>
